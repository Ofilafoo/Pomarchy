# Security

Omarchy plugins run unsandboxed with the current user's permissions. Pomarchy
must not require root privileges or contact the network at runtime.

Please report security issues privately to the project maintainer instead of
opening a public issue. A private reporting address will be added before the
first public release.

The project will document every external command, dependency, state path, and
systemd user unit before release.
